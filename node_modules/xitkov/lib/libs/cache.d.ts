import { Redis } from 'ioredis';
export declare class Cache {
    static client: Redis;
    static set(key: string, value: any, expirationInSeconds?: number): Promise<void>;
    static get(key: string): Promise<any>;
    static delete(key: string): Promise<void>;
}
