import { ElemBytes } from '../entry/elemBytes';
import { Entry } from '../../types/entry';
import { Bytes } from '../../types';
import { Data } from '../entry/data';
export declare const elemBytesToBigInts: (es: Array<ElemBytes>) => Array<bigint>;
export declare const checkEntryInField: (e: Entry) => boolean;
export declare const newDataFromBytes: (bytes: Bytes) => Data;
