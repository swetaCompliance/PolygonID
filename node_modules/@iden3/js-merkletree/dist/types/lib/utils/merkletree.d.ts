import { Path, Siblings } from '../../types/merkletree';
import { Bytes } from '../../types';
export declare const getPath: (numLevels: number, k: Bytes) => Path;
export declare const siblings2Bytes: (siblings: Siblings) => Bytes;
